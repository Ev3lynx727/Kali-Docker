# Docker-Host Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                             Internet/External                               │
└─────────────────────┬───────────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Firewalld Public Zone                              │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │ • Ports: 22(SSH), 80(HTTP), 443(HTTPS), 2376(Docker), 2377(Swarm)     │ │
│  │ • Masquerading: Enabled (NAT for container outbound traffic)           │ │
│  │ • Target: DROP (default deny, explicit allow)                          │ │
│  │ • Interface: eth0 (external network)                                   │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────┬───────────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                        Docker Host Container                                │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │ • CentOS Stream 9 base image                                           │ │
│  │ • systemd service manager                                              │ │
│  │ • firewalld network security                                           │ │
│  │ • Docker daemon with security hardening                                │ │
│  │ • SSH server (key-based auth only)                                     │ │
│  │ • Privileged container with NET_ADMIN, SYS_ADMIN capabilities         │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────┬───────────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         Firewalld Trusted Zone                             │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │ • Interface: docker0 (Docker bridge)                                  │ │
│  │ • Target: ACCEPT (container-to-host communication)                    │ │
│  │ • Masquerading: Disabled (direct routing)                             │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────┬───────────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Docker Networks                                   │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐             │
│  │ host-network    │  │ secure-net      │  │ isolated-net    │             │
│  │ 172.20.0.0/16  │  │ internal         │  │ internal        │             │
│  │ bridge          │  │ bridge          │  │ bridge          │             │
│  │ external access │  │ no external     │  │ no external     │             │
│  │ medium security │  │ high security   │  │ max security    │             │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘             │
└─────────────────────┬───────────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Container Services                                │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐             │
│  │ docker-host     │  │ security-mon.   │  │ log-aggregator  │             │
│  │ (controller)    │  │ (monitoring)    │  │ (fluent-bit)    │             │
│  │ host-network    │  │ host-network    │  │ host-network    │             │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘             │
│                                                                             │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐             │
│  │ user-containers │  │ databases       │  │ web services    │             │
│  │ (applications)  │  │ (secure-net)    │  │ (host-network)  │             │
│  │ various networks│  │ isolated        │  │ external access │             │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## Security Flow Summary

1. **External Traffic** → Firewalld Public Zone → Explicit Allow/Deny → Docker Host
2. **Container Traffic** → Docker Bridge → Firewalld Trusted Zone → Direct Routing
3. **Inter-Container** → Docker Networks → Bridge Communication → No Firewall Filtering
4. **Management Traffic** → SSH/API Ports → Authentication → Docker Daemon Control

## Key Security Boundaries

- **🔴 Public Zone**: Internet-facing, strict filtering, NAT for outbound
- **🟡 Trusted Zone**: Internal container traffic, direct routing, no NAT
- **🟢 Docker Networks**: Application isolation, bridge-based communication
- **🔵 Container Level**: Capability dropping, user namespaces, security options

## Data Flow Patterns

### Outbound Container Traffic
```
Container → docker0 → Trusted Zone → Public Zone → NAT → Internet
```

### Inbound Service Traffic
```
Internet → Public Zone → Port Filter → Docker Host → Port Mapping → Container
```

### Inter-Service Communication
```
Container A → Docker Network → Bridge → Container B
```

### Management Access
```
SSH/API → Public Zone → Authentication → Docker Host → Container Control
```

This architecture provides comprehensive security while maintaining operational flexibility for containerized applications.</content>
<parameter name="filePath">C:\Users\evely\Downloads\DEV\Docker-host\ARCHITECTURE-DIAGRAM.md